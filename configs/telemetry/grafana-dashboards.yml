# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: 'stratmaster-dashboards'
    orgId: 1
    folder: 'StratMaster'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards