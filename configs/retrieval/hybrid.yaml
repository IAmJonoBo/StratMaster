---
strategy: weighted_merge
alpha_dense: 0.6
alpha_sparse: 0.4
limit: 50
fallback:
  when_no_results: dense
pipelines:
  dense_config: ./colbert.yaml
  sparse_config: ./splade.yaml
  reranker_config: ./reranker.yaml
